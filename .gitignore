# === BMAD Framework (planning tooling, not shipped) ===
_bmad/
_bmad-output/

# === Claude Code ===
.claude/

# === OS ===
.DS_Store
Thumbs.db

# === Internal docs (personal notes, study guides, design sketches) ===
pipeline/docs/internal/

# === Python (pipeline/) ===
pipeline/.venv/
pipeline/.cache/
pipeline/cache/
pipeline/.env
pipeline/__pycache__/
pipeline/**/__pycache__/
pipeline/*.egg-info/
pipeline/src/*.egg-info/
pipeline/.mypy_cache/
*.py[cod]
*.so

# === Node (site/) ===
site/node_modules/
site/dist/
site/.astro/
site/.env.local
site/.env.*.local

# === Pipeline-generated data (regenerated by `make pipeline-full`) ===
# *.seed.json files are committed as fallback fixtures for fresh-clone builds.
site/src/data/*.json
!site/src/data/*.seed.json
site/public/data/
site/public/downloads/
site/public/tiles/*.pmtiles

# === Fallback globals (catch-all for future dirs) ===
.env
.env.*
!.env.example
dist/
build/

# === Secrets (anywhere) ===
*.pem
*.key
credentials.json

# === IDE ===
.idea/
.vscode/
*.swp
*.swo
*~
